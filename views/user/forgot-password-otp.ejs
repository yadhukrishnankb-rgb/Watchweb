<!-- <!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Verify OTP</title>
<script>
let timer = <%= timer %>;
function startTimer() {
  const timerSpan = document.getElementById('timer');
  const resendBtn = document.getElementById('resendBtn');
  resendBtn.disabled = true;
  let interval = setInterval(() => {
    if (timer <= 0) {
      clearInterval(interval);
      timerSpan.textContent = "00:00";
      resendBtn.disabled = false;
    } else {
      let min = String(Math.floor(timer / 60)).padStart(2, '0');
      let sec = String(timer % 60).padStart(2, '0');
      timerSpan.textContent = min + ":" + sec;
      timer--;
    }
  }, 1000);
}
window.onload = startTimer;
</script>
</head>
<body>
<div>
  <h2>Enter OTP</h2>
  <% if (message) { %><div><%= message %></div><% } %>
  <form action="/forgot-password-otp" method="POST">
    <input type="text" name="otp" maxlength="6" required placeholder="Enter OTP">
    <button type="submit">Verify OTP</button>
  </form>
  <div>
    <span>OTP expires in <span id="timer"></span></span>
  </div>
  <form action="/resend-otp" method="POST">
    <button type="submit" id="resendBtn" disabled>Resend OTP</button>
  </form>
</div>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Verify OTP</title>
<style>
    :root {
        --first-color: hsl(176, 88%, 27%);
        --first-color-alt: hsl(129, 44%, 94%);
        --second-color: hsl(34, 94%, 87%);
        --title-color: hsl(0, 0%, 13%);
        --text-color: hsl(154, 13%, 32%);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, var(--first-color-alt), var(--second-color));
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }

    div {
        background: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    div::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--first-color), var(--second-color));
    }

    h2 {
        color: var(--title-color);
        margin-bottom: 10px;
        font-size: 28px;
        font-weight: 600;
        position: relative;
    }

    h2::after {
        content: '';
        display: block;
        width: 50px;
        height: 3px;
        background: var(--first-color);
        margin: 10px auto 20px;
        border-radius: 2px;
    }

    div > div {
        background: var(--first-color-alt);
        color: var(--text-color);
        padding: 12px 16px;
        border-radius: 10px;
        margin-bottom: 20px;
        border-left: 4px solid var(--first-color);
        font-size: 14px;
        text-align: left;
    }

    form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-bottom: 25px;
    }

    input[type="text"] {
        padding: 16px 20px;
        border: 2px solid var(--first-color-alt);
        border-radius: 12px;
        font-size: 18px;
        color: var(--text-color);
        background: var(--first-color-alt);
        transition: all 0.3s ease;
        outline: none;
        text-align: center;
        letter-spacing: 2px;
        font-weight: 600;
    }

    input[type="text"]:focus {
        border-color: var(--first-color);
        background: white;
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }

    input[type="text"]::placeholder {
        color: var(--text-color);
        opacity: 0.7;
        letter-spacing: normal;
        font-weight: normal;
    }

    button {
        padding: 16px 20px;
        background: linear-gradient(135deg, var(--first-color), hsl(176, 88%, 35%));
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: left 0.5s ease;
    }

    button:hover:not(:disabled) {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    }

    button:hover:not(:disabled)::before {
        left: 100%;
    }

    button:active:not(:disabled) {
        transform: translateY(0);
    }

    button:disabled {
        background: linear-gradient(135deg, hsl(0, 0%, 70%), hsl(0, 0%, 80%));
        cursor: not-allowed;
        transform: none;
        box-shadow: none;
    }

    div > div:nth-of-type(2) {
        background: var(--first-color-alt);
        color: var(--text-color);
        padding: 15px 20px;
        border-radius: 12px;
        margin-bottom: 20px;
        border: 2px solid var(--first-color);
        border-style: dashed;
        text-align: center;
        font-size: 14px;
        font-weight: 500;
    }

    #timer {
        color: var(--first-color);
        font-weight: 700;
        font-size: 16px;
        font-family: 'Courier New', monospace;
        background: white;
        padding: 4px 8px;
        border-radius: 6px;
        margin-left: 5px;
        border: 1px solid var(--first-color);
    }

    form:last-child {
        margin-bottom: 0;
    }

    form:last-child button {
        background: linear-gradient(135deg, var(--second-color), hsl(34, 94%, 80%));
        color: var(--text-color);
        font-size: 14px;
        padding: 12px 20px;
    }

    form:last-child button:hover:not(:disabled) {
        background: linear-gradient(135deg, hsl(34, 94%, 80%), var(--second-color));
    }

    form:last-child button:disabled {
        background: linear-gradient(135deg, hsl(0, 0%, 90%), hsl(0, 0%, 95%));
        color: hsl(0, 0%, 60%);
    }

    @media (max-width: 480px) {
        div {
            padding: 30px 20px;
            margin: 10px;
        }
        
        h2 {
            font-size: 24px;
        }
        
        input[type="text"], button {
            padding: 14px 16px;
            font-size: 14px;
        }
        
        input[type="text"] {
            font-size: 16px;
        }
        
        div > div:nth-of-type(2) {
            padding: 12px 16px;
        }
    }
</style>
<script>
let timer = <%= timer %>;
function startTimer() {
  const timerSpan = document.getElementById('timer');
  const resendBtn = document.getElementById('resendBtn');
  resendBtn.disabled = true;
  let interval = setInterval(() => {
    if (timer <= 0) {
      clearInterval(interval);
      timerSpan.textContent = "00:00";
      resendBtn.disabled = false;
    } else {
      let min = String(Math.floor(timer / 60)).padStart(2, '0');
      let sec = String(timer % 60).padStart(2, '0');
      timerSpan.textContent = min + ":" + sec;
      timer--;
    }
  }, 1000);
}
window.onload = startTimer;
</script>
</head>
<body>
<div>
  <h2>Enter OTP</h2>
  <% if (message) { %><div><%= message %></div><% } %>
  <form action="/forgot-password-otp" method="POST">
    <input type="text" name="otp" maxlength="6" required placeholder="Enter OTP">
    <button type="submit">Verify OTP</button>
  </form>
  <div>
    <span>OTP expires in <span id="timer"></span></span>
  </div>
  <form action="/resend-otp" method="POST">
    <button type="submit" id="resendBtn" disabled>Resend OTP</button>
  </form>
</div>
</body>
</html>